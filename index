<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Site avec Bords Brillants</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      height: 100vh;
      background-color: #000;
      overflow: hidden;
      position: relative;
      font-family: Arial, sans-serif;
    }

    /* Bordures brillantes */
    .glow-border {
      position: absolute;
      z-index: 1;
    }

    .glow-top, .glow-bottom {
      width: 100%;
      height: 10px;
      left: 0;
    }

    .glow-top {
      top: 0;
      box-shadow: 0 0 20px 10px rgba(0, 255, 255, 0.8);
    }

    .glow-bottom {
      bottom: 0;
      box-shadow: 0 0 20px 10px rgba(0, 255, 255, 0.8);
    }

    .glow-left, .glow-right {
      width: 10px;
      height: 100%;
      top: 0;
    }

    .glow-left {
      left: 0;
      box-shadow: 0 0 20px 10px rgba(0, 255, 255, 0.8);
    }

    .glow-right {
      right: 0;
      box-shadow: 0 0 20px 10px rgba(0, 255, 255, 0.8);
    }

    /* Contenu central (optionnel) */
    .content {
      position: relative;
      z-index: 2;
      color: white;
      text-align: center;
      padding: 50px;
    }
  </style>
</head>
<body>
  <div class="glow-border glow-top"></div>
  <div class="glow-border glow-bottom"></div>
  <div class="glow-border glow-left"></div>
  <div class="glow-border glow-right"></div>

  <div class="content">
    <h1>Bienvenue sur mon site</h1>
    <p>Voici un site avec un fond noir et des bords brillants !</p>
  </div>
</body>
</html>
